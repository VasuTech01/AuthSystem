<!DOCTYPE html>
<html lang="en">
<head>
   {{>meta}}
</head>
<body>
  {{>header}}
   {{!-- {{#if username}}
    {{>header}}
    {{else}}
    {{>navbar parameter=username}}
   {{/if}} --}}
  
   <h1>{{username}}</h1>
     {{email}}
 {{>AddnScripts}}
</body>
</html>